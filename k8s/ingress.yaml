apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-data-exchange
  namespace: sm-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sm-data-exchange.sm-platform.example.com
      secretName: sm-data-exchange-tls
  rules:
    - host: sm-data-exchange.sm-platform.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-data-exchange
                port:
                  number: 8300
